# CAN Spec

This page is replaced by the CAN spec app when the documentation build copies the Vite bundle into `site/CAN-Spec/`. If you use `mkdocs serve` before that step, run `./scripts/build-docs-with-canspec-ui.sh` or work in `can/canspec-ui` with `npm run dev`.
