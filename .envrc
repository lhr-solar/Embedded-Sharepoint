# direnv: auto-load the Nix dev shell on `cd` into this repo.
# Install direnv (https://direnv.net) then run `direnv allow` once.
# Falls back silently if `use flake` is unavailable.
use flake || true
