name: Initialize ClickUp from GitHub Issues

on:
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Run ClickUp task initialization using GitHub Issues
        env:
          GITHUB_REPO: lhr-solar/Embedded-Sharepoint
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN_PAT }}
          CLICKUP_API_TOKEN: ${{ secrets.CLICKUP_API_TOKEN }}
          CLICKUP_LIST_ID: ${{ secrets.CLICKUP_LIST_ID }}
        run: |
          python .github/clickup-init.py
